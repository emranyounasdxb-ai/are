"use client";

import { useQuery, useQueryClient } from "@tanstack/react-query";
import { Archive, ArrowLeft, Plus, Save, Send, Trash2, Upload } from "lucide-react";
import { useRouter } from "next/navigation";
import { useRef, useState } from "react";

import { api, type PageResponse, type ResourceRecord } from "../lib/api";
import { AdminPageHeader, ErrorSummary, FormSection, InlineFeedback, LanguageTabs, LoadingState, StatusBadge, StickyFormActions } from "./admin-ui";
import { useAuth } from "./auth-provider";
import { GuardedLink } from "./navigation-guard";

type Translation = { official_name: string; short_summary: string; full_description: string; seo_title: string; seo_description: string };
type Source = { id?: string; source_url: string; source_type: string; is_official: boolean; retrieved_at: string; last_checked_at: string; content_hash: string; source_title: string; source_developer_domain: string; is_active: boolean };
type Media = { id?: string; category: string; source_url: string; rights_status: string; alt_en: string; alt_ar: string; display_order: number; verified_at: string; has_upload?: boolean };
type Milestone = { sequence: number; stage: string; label_en: string; label_ar: string; percentage: string; due_trigger: string; source_value: string };
type Values = { slug: string; developer_id: string; area_id: string; status: string; availability_status: string; construction_status: string; handover_quarter: string; handover_year: string; original_handover_value: string; last_verified_at: string; priority: string; featured: boolean; display_order: number; internal_notes: string; property_types: string[]; bedroom_options: string[]; translations: { en: Translation; ar: Translation }; sources: Source[]; payment_raw: string; payment_source_index: number; payment_complete: boolean; payment_verified_at: string; milestones: Milestone[]; media: Media[] };
type OptionRecord = ResourceRecord & { translations?: Record<string, { name?: string }>; name_en?: string };
type ProjectRecord = ResourceRecord & Partial<Values> & { translations?: Record<string, Translation>; sources?: Source[]; media?: Media[]; payment_plan?: { raw_source_text?: string; source_id?: string; is_complete?: boolean; verified_at?: string; milestones?: Milestone[] } | null };

const emptyTranslation = (): Translation => ({ official_name: "", short_summary: "", full_description: "", seo_title: "", seo_description: "" });
const emptySource = (): Source => ({ source_url: "", source_type: "OWNER_MANIFEST", is_official: false, retrieved_at: "", last_checked_at: "", content_hash: "", source_title: "", source_developer_domain: "", is_active: true });
const emptyMedia = (): Media => ({ category: "cover", source_url: "", rights_status: "pending", alt_en: "", alt_ar: "", display_order: 0, verified_at: "" });
const initialValues = (): Values => ({ slug: "", developer_id: "", area_id: "", status: "draft", availability_status: "coming-soon", construction_status: "not-confirmed", handover_quarter: "", handover_year: "", original_handover_value: "", last_verified_at: "", priority: "B", featured: false, display_order: 0, internal_notes: "", property_types: [], bedroom_options: [], translations: { en: emptyTranslation(), ar: emptyTranslation() }, sources: [], payment_raw: "", payment_source_index: 0, payment_complete: false, payment_verified_at: "", milestones: [], media: [] });

export function ProjectEditor({ id }: Readonly<{ id?: string }>) {
  const { user } = useAuth(); const router = useRouter(); const queryClient = useQueryClient(); const errorRef = useRef<HTMLDivElement>(null); const [locale, setLocale] = useState<"en" | "ar">("en"); const [values, setValues] = useState(initialValues); const [busy, setBusy] = useState(false); const [error, setError] = useState(""); const [notice, setNotice] = useState("");
  const record = useQuery({ queryKey: ["project", id], queryFn: async () => { const data = await api<ProjectRecord>(`/admin/projects/${id}`); setValues(recordValues(data)); return data; }, enabled: Boolean(id) });
  const developers = useQuery({ queryKey: ["project-developers"], queryFn: () => api<PageResponse<OptionRecord>>("/admin/developers?page_size=100") });
  const areas = useQuery({ queryKey: ["project-areas"], queryFn: () => api<PageResponse<OptionRecord>>("/admin/areas") });
  const set = <K extends keyof Values>(key: K, value: Values[K]) => setValues((current) => ({ ...current, [key]: value }));
  const setTranslation = (key: keyof Translation, value: string) => setValues((current) => ({ ...current, translations: { ...current.translations, [locale]: { ...current.translations[locale], [key]: value } } }));
  const payload = (status: string) => ({ slug: values.slug, developer_id: values.developer_id, area_id: values.area_id, status, availability_status: values.availability_status, construction_status: values.construction_status, handover_quarter: values.handover_quarter || null, handover_year: values.handover_year ? Number(values.handover_year) : null, original_handover_value: values.original_handover_value || null, last_verified_at: iso(values.last_verified_at), priority: values.priority, featured: values.featured, display_order: Number(values.display_order), internal_notes: values.internal_notes || null, property_types: values.property_types, bedroom_options: values.bedroom_options, translations: Object.fromEntries(Object.entries(values.translations).filter(([, item]) => item.official_name && item.short_summary && item.full_description && item.seo_title && item.seo_description)), sources: values.sources.filter((source) => source.source_url).map((source) => ({ ...source, retrieved_at: iso(source.retrieved_at), last_checked_at: iso(source.last_checked_at), source_title: source.source_title || null, source_developer_domain: source.source_developer_domain || null })), payment_plan: values.payment_raw ? { raw_source_text: values.payment_raw, source_index: values.payment_source_index, is_complete: values.payment_complete, verified_at: iso(values.payment_verified_at), milestones: values.milestones.map((item) => ({ ...item, percentage: item.percentage === "" ? null : Number(item.percentage), label_ar: item.label_ar || null, due_trigger: item.due_trigger || null })) } : null, media: values.media.filter((item) => item.source_url).map((item) => ({ ...item, alt_en: item.alt_en || null, alt_ar: item.alt_ar || null, verified_at: iso(item.verified_at), has_upload: undefined })) });
  async function save(status: string) { setBusy(true); setError(""); setNotice(""); try { const saved = await api<ProjectRecord>(id ? `/admin/projects/${id}` : "/admin/projects", { method: id ? "PUT" : "POST", body: JSON.stringify(payload(status)) }, user?.csrf_token); queryClient.setQueryData(["project", saved.id], saved); setNotice(status === "published" ? "Project published." : status === "archived" ? "Project archived." : "Draft saved."); if (!id) router.replace(`/projects/${saved.id}`); } catch (caught) { setError(caught instanceof Error ? caught.message : "The Project could not be saved."); requestAnimationFrame(() => errorRef.current?.focus()); } finally { setBusy(false); } }
  async function upload(mediaId: string, file?: File) { if (!file || !id) return; setBusy(true); try { const body = new FormData(); body.append("image", file); const saved = await api<ProjectRecord>(`/admin/projects/${id}/media/${mediaId}`, { method: "POST", body }, user?.csrf_token); queryClient.setQueryData(["project", id], saved); setNotice("Project media uploaded and sanitized."); } catch (caught) { setError(caught instanceof Error ? caught.message : "Upload failed."); } finally { setBusy(false); } }
  if (id && record.isLoading) return <LoadingState label="Loading Project…"/>; if (record.error) return <InlineFeedback tone="error">{record.error.message}</InlineFeedback>;
  const noAreas = !areas.isLoading && !(areas.data?.items.length); const englishComplete = complete(values.translations.en); const arabicComplete = complete(values.translations.ar);
  return <section><AdminPageHeader back={<GuardedLink className="back-link" href="/projects"><ArrowLeft aria-hidden size={16}/>Back to Projects</GuardedLink>} description="Projects are canonical Off-Plan developments. Prices are intentionally not part of this record." eyebrow="Off-Plan CMS" title={id ? "Review Project" : "New Project"} action={<StatusBadge status={values.status}/>}/><form className="editor-form" onSubmit={(event) => { event.preventDefault(); void save("draft"); }}><ErrorSummary focusRef={errorRef} message={error}/>{notice ? <InlineFeedback tone="success">{notice}</InlineFeedback> : null}{noAreas ? <InlineFeedback tone="info">No canonical Areas exist. ARE-PRJ-02 must verify and create an Area before a Project can be saved.</InlineFeedback> : null}<FormSection id="identity" title="1. Identity and workflow"><div className="form-grid"><label>Stable slug<input required pattern="[a-z0-9]+(?:-[a-z0-9]+)*" value={values.slug} onChange={(event) => set("slug", event.target.value)}/></label><label>Publication status<input disabled value={values.status}/></label><label>Display order<input min="0" type="number" value={values.display_order} onChange={(event) => set("display_order", Number(event.target.value))}/></label><label className="check"><input checked={values.featured} onChange={(event) => set("featured", event.target.checked)} type="checkbox"/>Featured</label></div></FormSection><FormSection id="relations" title="2. Developer and Area"><div className="form-grid"><label>Canonical Developer<select required value={values.developer_id} onChange={(event) => set("developer_id", event.target.value)}><option value="">Select Developer</option>{developers.data?.items.map((item) => <option key={item.id} value={item.id}>{item.translations?.en?.name ?? item.slug}</option>)}</select></label><label>Canonical Area/Community<select required value={values.area_id} onChange={(event) => set("area_id", event.target.value)}><option value="">Select verified Area</option>{areas.data?.items.map((item) => <option key={item.id} value={item.id}>{item.name_en}</option>)}</select></label></div></FormSection><FormSection id="content" title="3–4. English and Arabic content" description="Project names are never translated or transliterated automatically."><LanguageTabs active={locale} arabicComplete={arabicComplete} englishComplete={englishComplete} label="Project content language" onChange={setLocale}><div className="form-grid"><label>Official Project Name<input dir={locale === "ar" ? "rtl" : "ltr"} value={values.translations[locale].official_name} onChange={(event) => setTranslation("official_name", event.target.value)}/></label><label className="wide">Short summary<textarea rows={3} value={values.translations[locale].short_summary} onChange={(event) => setTranslation("short_summary", event.target.value)}/></label><label className="wide">Full description<textarea rows={6} value={values.translations[locale].full_description} onChange={(event) => setTranslation("full_description", event.target.value)}/></label><label>SEO title<input value={values.translations[locale].seo_title} onChange={(event) => setTranslation("seo_title", event.target.value)}/></label><label>SEO description<textarea rows={3} value={values.translations[locale].seo_description} onChange={(event) => setTranslation("seo_description", event.target.value)}/></label></div></LanguageTabs></FormSection><FormSection id="inventory" title="5. Property types and bedrooms"><Checkboxes label="Property types" options={["apartment", "villa", "townhouse", "penthouse", "duplex", "mansion", "residential-plot", "other"]} selected={values.property_types} onChange={(selected) => set("property_types", selected)}/><Checkboxes label="Bedroom options" options={["studio", "1", "2", "3", "4", "5", "6+"]} selected={values.bedroom_options} onChange={(selected) => set("bedroom_options", selected)}/></FormSection><FormSection id="handover" title="6. Handover"><div className="form-grid"><label>Quarter<select value={values.handover_quarter} onChange={(event) => set("handover_quarter", event.target.value)}><option value="">Not confirmed</option>{["Q1", "Q2", "Q3", "Q4"].map((item) => <option key={item}>{item}</option>)}</select></label><label>Year<input min="2000" max="2200" type="number" value={values.handover_year} onChange={(event) => set("handover_year", event.target.value)}/></label><label className="wide">Original source value<input value={values.original_handover_value} onChange={(event) => set("original_handover_value", event.target.value)}/></label></div></FormSection><PaymentSection values={values} set={set}/><FormSection id="status" title="8. Availability and construction"><div className="form-grid"><label>Availability<select value={values.availability_status} onChange={(event) => set("availability_status", event.target.value)}>{["available", "limited-availability", "sold-out", "coming-soon"].map((item) => <option key={item} value={item}>{human(item)}</option>)}</select></label><label>Construction<select value={values.construction_status} onChange={(event) => set("construction_status", event.target.value)}>{["pre-launch", "launched", "under-construction", "near-completion", "completed", "on-hold", "not-confirmed"].map((item) => <option key={item} value={item}>{human(item)}</option>)}</select></label></div></FormSection><SourceSection values={values} set={set}/><MediaSection busy={busy} id={id} onUpload={upload} values={values} set={set}/><FormSection id="priority" title="12. Internal priority and notes" description="ARE Priority and internal notes never enter the Public API."><div className="form-grid"><label>ARE Priority<select value={values.priority} onChange={(event) => set("priority", event.target.value)}><option value="A">A — Homepage/high focus</option><option value="B">B — Standard visibility</option><option value="C">C — Reference</option></select></label><label>Last Verified<input type="datetime-local" value={values.last_verified_at} onChange={(event) => set("last_verified_at", event.target.value)}/></label><label className="wide">Internal notes<textarea rows={4} value={values.internal_notes} onChange={(event) => set("internal_notes", event.target.value)}/></label></div></FormSection><div id="preview"><StickyFormActions help="Publication requires bilingual content, authoritative sources, a published Area/Developer and approved uploaded cover." state={id ? "Canonical Project record" : "Not saved yet"}><GuardedLink className="secondary-button" href="/projects">Cancel</GuardedLink><button className="action-button" disabled={busy || noAreas} type="submit"><Save aria-hidden size={16}/>Save Draft</button><button className="action-button action-button--publish" disabled={busy || !id} onClick={() => void save("published")} type="button"><Send aria-hidden size={16}/>Publish</button>{id && values.status !== "archived" ? <button className="action-button action-button--archive" disabled={busy} onClick={() => void save("archived")} type="button"><Archive aria-hidden size={16}/>Archive</button> : null}</StickyFormActions></div></form></section>;
}

function PaymentSection({ values, set }: Readonly<{ values: Values; set: <K extends keyof Values>(key: K, value: Values[K]) => void }>) { const update = (index: number, key: keyof Milestone, value: string | number) => set("milestones", values.milestones.map((item, itemIndex) => itemIndex === index ? { ...item, [key]: value } : item)); return <FormSection id="payment" title="7. Payment plan" description="Preserve source wording. Never infer a missing percentage or Arabic label."><div className="form-grid"><label className="wide">Raw source wording<textarea rows={3} value={values.payment_raw} onChange={(event) => set("payment_raw", event.target.value)}/></label><label>Source row index<input min="0" type="number" value={values.payment_source_index} onChange={(event) => set("payment_source_index", Number(event.target.value))}/></label><label>Verified at<input type="datetime-local" value={values.payment_verified_at} onChange={(event) => set("payment_verified_at", event.target.value)}/></label><label className="check"><input checked={values.payment_complete} onChange={(event) => set("payment_complete", event.target.checked)} type="checkbox"/>Plan is complete</label></div><div className="block-list"><div className="block-list-heading"><strong>Ordered milestones</strong><button onClick={() => set("milestones", [...values.milestones, { sequence: values.milestones.length + 1, stage: "other", label_en: "", label_ar: "", percentage: "", due_trigger: "", source_value: "" }])} type="button"><Plus aria-hidden size={15}/>Add milestone</button></div>{values.milestones.map((item, index) => <div className="content-block" key={`milestone-${index}`}><div className="form-grid"><label>Sequence<input min="0" type="number" value={item.sequence} onChange={(event) => update(index, "sequence", Number(event.target.value))}/></label><label>Stage<select value={item.stage} onChange={(event) => update(index, "stage", event.target.value)}>{["booking", "during-construction", "handover", "post-handover", "other"].map((stage) => <option key={stage} value={stage}>{human(stage)}</option>)}</select></label><label>English label<input value={item.label_en} onChange={(event) => update(index, "label_en", event.target.value)}/></label><label>Arabic label<input dir="rtl" value={item.label_ar} onChange={(event) => update(index, "label_ar", event.target.value)}/></label><label>Percentage<input min="0" max="100" step="0.01" type="number" value={item.percentage} onChange={(event) => update(index, "percentage", event.target.value)}/></label><label>Timing/event<input value={item.due_trigger} onChange={(event) => update(index, "due_trigger", event.target.value)}/></label><label className="wide">Original source value<input value={item.source_value} onChange={(event) => update(index, "source_value", event.target.value)}/></label></div><button onClick={() => set("milestones", values.milestones.filter((_, itemIndex) => itemIndex !== index))} type="button"><Trash2 aria-hidden size={15}/>Remove milestone</button></div>)}</div></FormSection>; }
function SourceSection({ values, set }: Readonly<{ values: Values; set: <K extends keyof Values>(key: K, value: Values[K]) => void }>) { const update = (index: number, key: keyof Source, value: string | boolean) => set("sources", values.sources.map((item, itemIndex) => itemIndex === index ? { ...item, [key]: value } : item)); const types = ["OWNER_MANIFEST", "DLD_PROJECT_STATUS", "OFFICIAL_DEVELOPER_PAGE", "OFFICIAL_DEVELOPER_BROCHURE", "OFFICIAL_MASTER_COMMUNITY_PAGE", "OWNER_SUPPLIED_DOCUMENT", "OWNER_APPROVED_PARTNER_FEED", "APPROVED_SECONDARY_SOURCE"]; return <FormSection id="sources" title="9. Sources and verification" description="Lower-authority sources cannot silently override official evidence."><div className="block-list-heading"><strong>Evidence sources</strong><button onClick={() => set("sources", [...values.sources, emptySource()])} type="button"><Plus aria-hidden size={15}/>Add source</button></div>{values.sources.map((source, index) => <div className="content-block" key={`source-${index}`}><div className="form-grid"><label>Source type<select value={source.source_type} onChange={(event) => update(index, "source_type", event.target.value)}>{types.map((item) => <option key={item}>{item}</option>)}</select></label><label>Source URL<input dir="ltr" type="url" value={source.source_url} onChange={(event) => update(index, "source_url", event.target.value)}/></label><label>Retrieved<input type="datetime-local" value={source.retrieved_at} onChange={(event) => update(index, "retrieved_at", event.target.value)}/></label><label>Last checked<input type="datetime-local" value={source.last_checked_at} onChange={(event) => update(index, "last_checked_at", event.target.value)}/></label><label className="wide">SHA-256 content hash<input dir="ltr" maxLength={64} value={source.content_hash} onChange={(event) => update(index, "content_hash", event.target.value)}/></label><label>Source title<input value={source.source_title} onChange={(event) => update(index, "source_title", event.target.value)}/></label><label>Developer/domain<input value={source.source_developer_domain} onChange={(event) => update(index, "source_developer_domain", event.target.value)}/></label><label className="check"><input checked={source.is_official} onChange={(event) => update(index, "is_official", event.target.checked)} type="checkbox"/>Official classification</label><label className="check"><input checked={source.is_active} onChange={(event) => update(index, "is_active", event.target.checked)} type="checkbox"/>Source active</label></div><button onClick={() => set("sources", values.sources.filter((_, itemIndex) => itemIndex !== index))} type="button"><Trash2 aria-hidden size={15}/>Remove source</button></div>)}</FormSection>; }
function MediaSection({ values, set, id, busy, onUpload }: Readonly<{ values: Values; set: <K extends keyof Values>(key: K, value: Values[K]) => void; id?: string; busy: boolean; onUpload: (mediaId: string, file?: File) => Promise<void> }>) { const update = (index: number, key: keyof Media, value: string | number) => set("media", values.media.map((item, itemIndex) => itemIndex === index ? { ...item, [key]: value } : item)); const categories = ["cover", "gallery", "exterior", "interior", "amenities", "floor-plan", "master-plan", "location-map", "construction", "video-reference"]; return <FormSection id="media" title="10. Media" description="No Project may publish without an uploaded approved cover and bilingual alternative text."><div className="block-list-heading"><strong>Project media</strong><button onClick={() => set("media", [...values.media, emptyMedia()])} type="button"><Plus aria-hidden size={15}/>Add media</button></div>{values.media.map((media, index) => <div className="content-block" key={media.id ?? `media-${index}`}><div className="form-grid"><label>Category<select value={media.category} onChange={(event) => update(index, "category", event.target.value)}>{categories.map((item) => <option key={item} value={item}>{human(item)}</option>)}</select></label><label>Rights status<select value={media.rights_status} onChange={(event) => update(index, "rights_status", event.target.value)}><option value="pending">Pending</option><option value="approved">Approved</option><option value="rejected">Rejected</option></select></label><label className="wide">Provenance/source URL<input dir="ltr" type="url" value={media.source_url} onChange={(event) => update(index, "source_url", event.target.value)}/></label><label>English alt text<input value={media.alt_en} onChange={(event) => update(index, "alt_en", event.target.value)}/></label><label>Arabic alt text<input dir="rtl" value={media.alt_ar} onChange={(event) => update(index, "alt_ar", event.target.value)}/></label><label>Display order<input min="0" type="number" value={media.display_order} onChange={(event) => update(index, "display_order", Number(event.target.value))}/></label><label>Verified at<input type="datetime-local" value={media.verified_at} onChange={(event) => update(index, "verified_at", event.target.value)}/></label>{id && media.id ? <label>Upload JPEG, PNG or WebP<span className="secondary-button"><Upload aria-hidden size={15}/>{media.has_upload ? "Replace file" : "Upload file"}</span><input className="visually-hidden" accept="image/jpeg,image/png,image/webp" disabled={busy} onChange={(event) => void onUpload(media.id!, event.target.files?.[0])} type="file"/></label> : <p>Save the Draft before uploading a file.</p>}</div><button onClick={() => set("media", values.media.filter((_, itemIndex) => itemIndex !== index))} type="button"><Trash2 aria-hidden size={15}/>Remove metadata</button></div>)}</FormSection>; }
function Checkboxes({ label, options, selected, onChange }: Readonly<{ label: string; options: string[]; selected: string[]; onChange: (selected: string[]) => void }>) { return <div className="field-group"><span className="field-label">{label}</span><div className="check-grid">{options.map((item) => <label className="check" key={item}><input checked={selected.includes(item)} onChange={(event) => onChange(event.target.checked ? [...selected, item] : selected.filter((value) => value !== item))} type="checkbox"/>{human(item)}</label>)}</div></div>; }
function recordValues(record: ProjectRecord): Values { const plan = record.payment_plan; return { ...initialValues(), ...record, status: record.status ?? "draft", handover_year: record.handover_year ? String(record.handover_year) : "", last_verified_at: localDateTime(record.last_verified_at), translations: { en: record.translations?.en ?? emptyTranslation(), ar: record.translations?.ar ?? emptyTranslation() }, sources: record.sources?.map((item) => ({ ...item, retrieved_at: localDateTime(item.retrieved_at), last_checked_at: localDateTime(item.last_checked_at) })) ?? [], media: record.media?.map((item) => ({ ...item, verified_at: localDateTime(item.verified_at) })) ?? [], payment_raw: plan?.raw_source_text ?? "", payment_source_index: Math.max(0, (record.sources ?? []).findIndex((source) => source.id === plan?.source_id)), payment_complete: Boolean(plan?.is_complete), payment_verified_at: localDateTime(plan?.verified_at), milestones: plan?.milestones?.map((item) => ({ ...item, percentage: item.percentage == null ? "" : String(item.percentage) })) ?? [] }; }
function complete(value: Translation) { return Object.values(value).every((item) => item.trim().length >= 2); }
function human(value: string) { return value.split("-").map((word) => word.charAt(0).toUpperCase() + word.slice(1)).join(" "); }
function iso(value?: string | null) { return value ? new Date(value).toISOString() : null; }
function localDateTime(value?: string | null) { return value ? new Date(value).toISOString().slice(0, 16) : ""; }
